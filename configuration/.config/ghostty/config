# other config files (? means optional)
config-file = ?keymap
config-file = ?theming

mouse-hide-while-typing = true
desktop-notifications = true

shell-integration = zsh
shell-integration-features = cursor,sudo,title
