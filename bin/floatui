#!/usr/bin/bash

# FLOATUI
# create some floating terminals for TUIs
#

# Lookup-Table for Choice to command to launch program
declare -rA LUT=(
    [audio]="wiremix"
    [bluetooth]="bluetui"
    [calc]="qalc"
    [files]="yazi $HOME"
    [ipscan]="whosthere"
    [jellyfin]="jellyfin-tui"
    [mastodon]="toot tui"
    [matrix]="iamb"
    [memo]="$HOME/.local/bin/memo"
    [monitor]="monitui"
    [music]="kew ."
    [notes]="$HOME/.local/bin/notes"
    [timer]="timr"
    [wifi]="sudo impala"
)

choice=$1
cmd=${LUT[${choice}]}

# check if the key was found
if test -z "${cmd}"; then
    echo "Key ${choice} does not exist"
    exit 1
fi

# echo ${LUT[${choice}]}

# execute command
(eval "kitty --class floatui-${choice} -e ${cmd}") &
disown
